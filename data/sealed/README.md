# Sealed-test boundary

The sealed split is materialized under
`experiments/datasets/v0.3-small/sealed-test.jsonl` by the deterministic
generator. This directory records the boundary without copying the sealed
rows. Before a formal empirical release, commit the preregistration and the
sealed-data manifest before inspecting final model outputs.
